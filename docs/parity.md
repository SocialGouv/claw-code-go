# Parity matrix vs Claude Code

Snapshot as of 2026-04-28, after the multi-phase port session.

Rating legend:

- **COMPLETE** — feature is wired end-to-end and exercised by tests or live runs.
- **PARTIAL** — the substrate ships, but at least one user-visible surface (broker, exporter, UI, etc.) is still pending. See notes.
- **MISSING** — placeholder or types may exist, but the user-visible feature is not callable.

| Capability axis | claude_code (Anthropic CLI) | claw-code-go | Rating | Notes |
|-----------------|------------------------------|--------------|--------|-------|
| Built-in tool surface (read_file, write_file, bash, glob, grep, file_edit, web_fetch) | yes | yes | COMPLETE | `pkg/api/tools/builtins.go`, `internal/tools/*.go`. Bash gates on the permissions package. |
| Slash commands / skills | yes | yes | COMPLETE | Skill registry: `internal/tools/skill.go`. Slash dispatcher: `internal/commands/dispatcher.go`, `internal/commands/slash_test.go`. |
| MCP (stdio + SSE + websocket clients, server SDK) | yes | yes | COMPLETE | Transport: `internal/mcp/{stdio,sse,websocket,transport_rpc}.go`. Server SDK: `internal/mcp/sdk_server.go`. Lifecycle: `internal/mcp/lifecycle.go`. OAuth broker plugs into `TransportConfig.AuthFunc` (see below). |
| MCP OAuth (authorize, refresh, store) | yes | yes | COMPLETE | Auth-code + PKCE broker on top of the atomic disk storage: `internal/mcp/oauth/{broker.go,pkce.go,storage.go}`. Public façade re-exports `Broker`, `NewBroker`, `Token`, `Storage`, and the typed `ErrReauthRequired` for headless callers: `pkg/api/mcp/oauth/oauth.go`. Tests: `internal/mcp/oauth/{broker_test.go,pkce_test.go}` (RFC 6749 / 7636 happy path, refresh rotation, invalid_grant → ErrReauthRequired, transient 5xx, state-mismatch CSRF reject, revoke clears local cache on remote failure). Transports consume the broker via `broker.BearerHeaderFunc(cfg)` → `TransportConfig.AuthFunc`. |
| Lifecycle hooks (PreToolUse, PostToolUse, UserPromptSubmit, PreCompact, PostCompact, Stop, Plugin Pre/Post Install + Uninstall) | yes | yes | COMPLETE | In-process Runner shipped and integrated: `internal/hooks/runner.go`, used by `internal/runtime/conversation.go` and `internal/runtime/compact.go`. Conversation `context.Context` now flows into both `Runner.Fire` and the shell `HookRunner.Run*` methods, so cancellation propagates to lifecycle handlers and to running hook scripts (`exec.CommandContext`). Plugin lifecycle hooks (`PrePluginInstall`, `PostPluginInstall`, `PrePluginUninstall`, `PostPluginUninstall`) fire from `plugin/manager.go` via the `WithHooks` option; a Pre Block aborts the operation, Post fires on success and failure with `Plugin.Error` set on failure. |
| Session (jsonl persistence, fork, inherit) | yes | yes | COMPLETE | `internal/runtime/session.go`, `internal/runtime/session_jsonl.go`, `internal/runtime/session_store.go`. JSONL turn-by-turn replay validated. |
| Sub-agents / team delegation | yes | yes | COMPLETE | `internal/runtime/team/registry.go`, `internal/tools/agent_test.go`, `internal/tools/team_tools_test.go`. |
| Providers — anthropic + openai | yes | yes | COMPLETE | `pkg/api/providers/{anthropic,openai}`. OpenAI covers both `/v1/chat/completions` and `/v1/responses` (`internal/api/providers/openai/responses.go`); the responses translator handles parallel reasoning + tool_calls without dropping interleaved text or argument deltas. |
| Providers — Bedrock / Vertex / Foundry | yes | available, live tests in repo (gated by env) | PARTIAL | Real implementations: `pkg/api/providers/{bedrock,vertex,foundry}/provider.go` + `internal/api/providers/{bedrock,vertex,foundry}`. Compile + unit tests pass. Live smoke tests under build tag `live`: `internal/api/providers/{bedrock,vertex,foundry}/provider_live_test.go` — skip cleanly without creds, exercise auth + streaming when the documented env vars are set (see "Running live provider tests" in the README). |
| Permissions (mode + allow/deny rules + prompter) | yes (5 modes) | yes (7 modes + Classifier) | COMPLETE | All 7 modes shipped: `internal/permissions/mode.go`. `Classifier` interface: `internal/permissions/classifier.go`. Two classifiers ship: the default rule-based `RuleClassifier` (read-only safe-list) and a small-model `LLMClassifier` (`internal/permissions/llm_classifier.go`) that mirrors what Claude Code uses, with a TTL+FIFO decision cache and a fail-safe-to-Ask invariant on transport errors. Pre-wired manager helper: `NewLLMClassifierManager`. |
| Compaction (auto-trim history, preserve tool-call invariants) | yes | yes | COMPLETE | `internal/runtime/compact.go`, `internal/runtime/compact_test.go`. PreCompact/PostCompact hooks wired. |
| Agent SDK (programmatic conversation loop) | yes | yes | COMPLETE | `pkg/api/client.go` (`StreamResponse`), `internal/runtime/conversation.go`. Used by iterion via `model/generation.go`. |
| Vision / computer use | yes | yes | COMPLETE | `api.ImageSource` types: `internal/api/types.go`, `pkg/api/types.go`. Action dispatcher: `internal/tools/computer_use_exec.go` (`ComputerUseTool`, `ExecuteComputerUse`) re-exported from `pkg/api/tools/computer_use.go`. Action verbs mirror Anthropic's `computer_use_20241022` spec: `screenshot`, `left_click`, `right_click`, `middle_click`, `double_click`, `type`, `key`, `mouse_move`, `cursor_position`, `left_click_drag`. Backed by `xdotool` (input) + ImageMagick `import` (capture) on Linux/X11 — system deps, no Go cgo or extra modules. Returns the typed `ErrComputerUseUnavailable` (wrapped) when a display, binary, or supported OS is missing so callers can route the gap with `errors.Is` instead of string-matching. Unit tests use a runner-injected fake to exercise call shape without touching a real display: `internal/tools/computer_use_test.go`. |
| Telemetry (event taxonomy + exporter) | yes (OTLP) | yes (OTLP/HTTP + OTLP/gRPC) | COMPLETE | Event types: `internal/runtime/events.go`. Internal client telemetry: `internal/api/client_telemetry_test.go`. OTLP/HTTP-JSON exporter: `internal/apikit/telemetry/otlp/exporter.go`. OTLP/gRPC exporter (official `otlploggrpc` SDK): `internal/apikit/telemetry/otlpgrpc/exporter.go` — env var `CLAWD_OTLP_GRPC_ENDPOINT` enables wiring. JSONL session log remains the default offline sink. |
| Session timeline UI | yes (TUI render) | yes | COMPLETE | `claw-code-go timeline --session <id> [--format pretty\|json\|md] [--limit n]` renders saved JSONL sessions through the TUI markdown renderer (`internal/compat/timeline.go`). Reuses `runtime.LoadSessionAuto` and `tui.MarkdownRenderer`. |
| Plugin marketplace (remote discovery + install) | yes | yes | COMPLETE | Local plugin registry, manifest, and tool wiring: `plugin/{manager.go,manifest.go,registry.go,tool.go}`. Remote fetch + verify: `plugin/remote_marketplace.go` (`RemoteMarketplace` with `ListPlugins`/`FetchManifest`/`Install`) reuses the SHA-256 + cosign chain in `internal/plugins/`. CLI surface: `claw-code-go plugin install --marketplace <url> [--dest <dir>] [--require-signed] [--insecure-marketplace] <name>` (`internal/compat/plugin_install.go`). HTTPS-only by default; `index.json` + per-plugin `manifest.json` format documented in `docs/plugin_signing.md`. |
| Interactive / remote tools (`ask_user`, `remote_trigger`) | yes | yes | COMPLETE | `ask_user`: pluggable `Asker` interface with `StdinAsker`, `ProgrammaticAsker`, and `TUIAsker` (`internal/tools/ask_user.go`). Schema accepts structured `options` and `allow_free_text`. The streaming runtime path keeps using the `TurnEventAskUser` channel so the bundled TUI is unchanged; the non-streaming `ExecuteTool` path now consults `ConversationLoop.Asker`. Re-exported via `pkg/api/tools` (`AskUserQuestionTool`, `Asker`, `Question`, `Option`, `Answer`, `NewStdinAsker`, `NewProgrammaticAsker`, `ExecuteAskUser`, `ErrNoAsker`). `remote_trigger`: real `http.Client` wrapper with per-request timeout (default 30 s, max 5 min), response-body cap (default 1 MiB, ceiling 8 MiB) plus a `truncated` flag, input header blocklist (Cookie / Proxy-Authorization), Authorization header gated behind `RemoteTriggerOptions.AllowAuthHeader`, CRLF guard, http(s)-only URL scheme, and an output header allowlist that drops `Set-Cookie` before it reaches the model. Structured result: `status_code` / `success` / `body` / `truncated` / `headers` / `duration_ms`. (`internal/tools/remote_trigger.go`) |
| System-prompt section toggles / minimal-prompt mode | no (always-on prompt) | yes | COMPLETE | Every automatic prompt section (environment, git status, CLAUDE.md instructions, auto memory, posture, compaction summary, MCP tool list) is individually toggleable — heavy sections hurt small models. Tri-state `"prompt"` settings block merged field-wise across layers (`internal/config/{feature,loader,validate}.go`), CLI `--minimal-prompt` / `--prompt-sections` / `--disable-prompt-sections` (also on `print-system-prompt`), CLAUDE.md frontmatter `minimalPrompt`/`promptSections`/`disablePromptSections` keys. Defaults all-on. (`internal/runtime/config.go`, `internal/context/assembler.go`) |
| Memory: recursive CLAUDE.md walk-up + `@path` imports | yes | yes | COMPLETE | Discovery includes ancestor-directory CLAUDE.md files (root-most first) and expands `@path` imports (absolute, `~/`, relative) recursively — 5 hops max (Claude Code parity), cycle-safe, code fences and inline code spans skipped, frontmatter stripped from injection. Combined cap 48KB (`prompt.memoryMaxBytes`). (`internal/context/memory.go`, `internal/context/memory_test.go`) |
| Auto memory (persistent MEMORY.md) | yes | yes | COMPLETE | `# Auto memory` section injects the per-workspace memory dir (`~/.claw-code/memory/<fingerprint>/`, `CLAW_MEMORY_DIR` override), maintenance instructions, and the current MEMORY.md (8KB head-truncated). v1 = injection + instructions; the model maintains the file with the standard file tools (no dedicated memory tool yet). Off via `prompt.autoMemory=false`. (`internal/context/automemory.go`) |
| Operating-posture system prompt | yes (large native prompt) | yes (authored equivalent) | COMPLETE | `# Operating posture` section: concision, read-before-edit, targeted edits, convention-following, batched tool calls, git safety, verification honesty, stop-when-done. Original text targeting the same operating qualities — deliberately NOT a copy of Claude Code's prompt. Exposed to embedding hosts via `pkg/runtime.OperatingPosture()`; `pkg/runtime.BuildSystemContext()` renders context sections without it so hosts with their own posture (iterion) avoid doubling. (`internal/context/posture.go`, `pkg/runtime/prompt.go`) |
| Behavioral prompt sections (communication, task management, doing-tasks, tool policy, git safety, context management) | yes (inside the native prompt) | yes (authored equivalents) | COMPLETE | Six individually toggleable sections extend the posture to full behavioral coverage: final-message discipline + `file:line` references + `<system-reminder>` contract (`communication`), todo hygiene incl. post-compaction reseed (`task-management`), scope/objectivity/no-time-estimates (`doing-tasks`), parallel calls + dedicated-tools-over-shell + tool_search-before-unavailable (`tool-policy`), the git protocol (`git-safety`), and continuation-through-summarization + end-of-turn self-check (`context-management`). All original text. (`internal/context/sections.go`, wired in `internal/runtime/conversation.go`) |
| Tool descriptions teach usage policy | yes (long per-tool docs) | yes (authored, condensed) | COMPLETE | The 11 core tools (bash, read_file, write_file, file_edit, glob, grep, todo_write, agent, ask_user, tool_search, skill) carry short policy paragraphs: when to use which, read-before-edit, `old_string` uniqueness, stateless sub-agent prompts, ask-only-at-genuine-forks, tool_search-before-claiming-unavailable. (`internal/tools/*.go`) |
| `<system-reminder>` harness notices | yes (plan mode, compaction, memory, todo staleness…) | yes (seam + first producers) | COMPLETE | Producers queue notices; the loop flushes them as ONE injected user message at the next turn boundary (wire-safe, `IsInjected`, excluded from real-turn count). Shipped producers: post-compaction todo reseed, plan-mode enter/exit. (`internal/runtime/reminders.go`) |
| Compaction summary shape | yes (nine-section handoff skill) | yes (authored equivalent) | COMPLETE | Authored nine-section contract (`<analysis>`+`<summary>`; all user messages; security constraints verbatim; current work; optional next step anchored by a verbatim quote) replaces the old two-sentence prompt; summary budget 4096 tokens. Continuation message is an injected user message, never role `system` in `messages[]` (Anthropic wire rejects that). (`internal/runtime/compact.go`) |
| Read-only bash auto-allow | yes (built-in safe-list) | yes | COMPLETE | Verifiably read-only commands (conservative parse: redirection/substitution/heredoc/backgrounding reject; every chained segment must pass; git/gh/docker gated per subcommand, `gh api` non-GET detected) skip the prompt in prompting modes and in ModeAuto's RuleClassifier; deny rules and plan mode still win. Permission checks judge the full command, not the 60-char display summary. (`internal/permissions/readonly.go`) |
| `--system-prompt` / `--append-system-prompt` | yes | yes | COMPLETE | Replace-the-authored-base vs append-at-the-end semantics aligned with the claude CLI; on the main CLI and `print-system-prompt`; `/system-prompt set` now feeds the live prompt (was a dead write). (`cmd/claw-code-go/main.go`, `internal/runtime/config.go`) |
| Task graph (subject/activeForm/owner + blocks/blockedBy dependencies) | yes (Task* tools, build 2.1.172) | yes | COMPLETE | Registry tasks double as work items: reciprocal dependency edges, derived `blocked`/`work_status`, both status vocabularies (pending/in_progress/completed aliases), `status: "deleted"` removal with edge cleanup. `task_create`/`task_update`/`task_list` expose the graph; `todo_write` stays the flat session checklist sharing the vocabulary. (`internal/runtime/task/task.go`, `internal/tools/task_tools.go`) |
| Sub-agent spawning (background, typed, notified) | yes (Agent tool + named types) | yes | COMPLETE | The agent tool actually runs: child ConversationLoop in the background, transcript streamed into the task registry, completion announced via a queued `<system-reminder>` (no polling). Built-in types explore/plan/verification/general-purpose; background runs auto-deny permission asks and auto-answer ask_user with an unattended notice. (`internal/runtime/subagent.go`) |
| Runtime-defined subagent types | no (build-time agents only) | yes | COMPLETE | `define_subagent` registers a session-scoped type (system prompt + tool allow-list + model override), spawnable via `agent`; built-ins can't be shadowed; children never see orchestration tools. Beyond-parity (Antigravity-style). (`internal/tools/define_subagent.go`, `internal/runtime/subagent.go`) |
| Oracle advisor | no (Amp has it) | yes | COMPLETE | `oracle`: one read-only, tool-less call to `Config.OracleModel` (settings `oracleModel` / `CLAW_ORACLE_MODEL`; default = session model) with question + inlined files (48KB head cap with visible truncation markers). (`internal/tools/oracle.go`, `internal/runtime/oracle.go`) |
| Workflow orchestration DSL | yes (Workflow tool) | yes (v1) | COMPLETE | `workflow`: goja event-loop running plain-JS scripts; promise-returning `agent()` mapped onto the subagent/task machinery; `parallel()`/`pipeline()` prelude with the documented semantics (thrown stages drop items to null; pipeline has no barrier); `phase()`/`log()`/`args`; 200-agent/8-concurrent/timeout guardrails. No schema-forcing, budget, or resume yet — the v2 seams. (`internal/runtime/workflow.go`, `internal/tools/workflow.go`) |
| Semantic code search | no (Cursor has it) | yes (local v1) | COMPLETE | `semantic_search`: BM25 over overlapping line windows with identifier-split tokenization — relevance-ranked "where is X handled?" search with zero network/credentials; embeddings backend is the follow-on seam. Read-only: classifier auto-allowed, in the explore subagent set. (`internal/tools/semantic_search.go`) |

## Quick guidance for contributors

If you're adding a feature, the file:line citations above point to the package you'll most likely extend. For provider-touching work, prefer reusing `internal/api/httputil` and `internal/api/sseutil` rather than re-rolling HTTP/SSE plumbing — that's why they were extracted.

If you're chasing a "PARTIAL" rating to "COMPLETE":

